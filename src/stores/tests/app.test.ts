import { describe, it, expect } from 'vitest';
import { AppStore } from '../app';

describe('AppStore', () => {
  it('is defined', async () => {
    expect(AppStore).not.toBeNull();
  });
  // TODO: Add tests...
});
